// Escreva um algoritmo para imprimir os números de 5 a 15 utilizando uma estrutura ENQUANTO e um contador.

#include <stdio.h>
#include <conio.h>

main() {
    int c;
    c = 5;
    while (c <= 15) {
        printf("%d ", c);
        c++;
    }
}